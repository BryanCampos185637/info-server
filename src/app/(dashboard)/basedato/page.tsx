import { Container, Typography } from "@mui/material";

const page = () => {
  return (
    <Container>
      <Typography variant="h4">Base de datos</Typography>
    </Container>
  );
};

export default page;
