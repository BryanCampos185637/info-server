import { Container, Typography } from "@mui/material";

const page = () => {
  return (
    <Container>
      <Typography variant="h4">Servidores</Typography>
    </Container>
  );
};

export default page;
